package main

import (
	"fmt"
	//"math/rand"
	//"math/bits"
	//"sync"
	//"time"
	//"encoding/binary"
	//"encoding/hex"
)

/*
func process(ch chan int, wg sync.WaitGroup) {
	i := 0
	for {
		ch <- i
		if rand.Float64() >= .5 {
			time.Sleep(1 * time.Second)
		}
		wg.Add(1)
		wg.Done()
		i++

	}
}*/
func main() {
	for false {
		fmt.Println("in loop")
	}
	fmt.Println("out loop")

}
